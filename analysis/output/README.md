# analysis/output

Contains all **final results**: tables (`.tex`, `.txt`) and figures (`.eps`, `.pdf`).

- Do not manually place files here.
- All files are generated by `analysis/code/main.do`.
- Reference these directly from your paper in `paper/`.
