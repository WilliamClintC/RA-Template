# build/input

Place **raw, unmodified source data** here.

- Never edit or overwrite files in this folder.
- All cleaning happens in `build/code/` and outputs go to `build/output/`.
- Files here are inputs only; treat them as read-only.
